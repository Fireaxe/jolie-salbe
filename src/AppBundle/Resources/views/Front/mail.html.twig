<link href="{{ asset('css/app.css') }}" rel="stylesheet">

<div class="row">
    <div class="col-md-offset-1 col-md-11">
        <h1>Mail de : {{ name }}</h1>
        <ul>
            {% if phone %}
                <li>Tél: {{ phone }}</li>
            {% endif %}

            {% if email %}
                <li>Email: {{ email }}</li>
            {% endif %}
        </ul>

        <h2>Message :</h2>
        <p>
            {{ message|raw }}
        </p>
    </div>
</div>
