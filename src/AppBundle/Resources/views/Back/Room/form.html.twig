{% extends '::admin.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_row(form) }}

    <button class="btn btn-primary">Ajouter</button>
    {{ form_end(form) }}
{% endblock %}